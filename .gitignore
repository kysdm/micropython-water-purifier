# VS Code 本地配置（MicroPico 插件设置等，无需入库）
.vscode/

# VS Code MicroPython 插件自动生成的历史备份
.history/

# 设备运行时日志（会随固件运行产生，不入库）
logs/

# Python 缓存
__pycache__/
*.pyc

# 运行时配置：包含 WiFi 密码等敏感信息，由 config.py 在设备上自动生成
config.json

# 断电时钟备份文件（软件近似时钟）
time_state.txt

# 旧版 OTA 复制模式生成的更新包目录（默认根目录模式不需要）
ota/
